services:
  simple-wol:
    build: .
    network_mode: host
    cap_add:
      - NET_RAW
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - DATA_DIR=/data
      # - TZ=America/New_York  # time zone for scheduled wakes (defaults to UTC)
    restart: unless-stopped
