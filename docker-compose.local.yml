services:
  simple-wol:
    build: .
    ports:
      - "8080:8080"
    cap_add:
      - NET_RAW
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - DATA_DIR=/data
    restart: unless-stopped
